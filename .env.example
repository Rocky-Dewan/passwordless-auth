# File: passwordless-auth/.env.example
# Purpose: Example environment variables file for the passwordless-auth service.
# NOTE: In a production environment, secrets should be managed via a dedicated
# secret manager (e.g., AWS Secrets Manager, Vault, Kubernetes Secrets).

# --- Application Configuration ---
NODE_ENV=development
PORT=3000
APP_NAME="SecureAuth"
BASE_URL="http://localhost:3000"

# --- Database Configuration (PostgreSQL) ---
# Used by the application and docker-compose
DB_HOST=db
DB_PORT=5432
DB_USER=auth_user
DB_PASSWORD=secure_password # CHANGE ME
DB_NAME=passwordless_auth_db

# --- Redis Configuration ---
# Used for caching, rate limiting, and temporary magic link codes
REDIS_HOST=redis
REDIS_PORT=6379

# --- Security Secrets ---
# MUST be long, random strings (e.g., 64 characters or more).
# Use a tool like 'openssl rand -base64 64' to generate.
JWT_SECRET="a_very_long_and_complex_jwt_secret_key_for_production_use_1234567890abcdef" # CHANGE ME
CSRF_SECRET="a_very_long_and_complex_csrf_secret_key_for_production_use_fedcba0987654321" # CHANGE ME

# --- Email Service Configuration ---
# Replace with your actual SendGrid, Mailgun, or other SMTP credentials
EMAIL_SENDER="no-reply@secureauth.com"
EMAIL_PROVIDER_API_KEY="your_email_api_key" # Uncomment and set in production

# --- IP Geolocation Service ---
# Using a free service for example. Replace with a paid, rate-limit-friendly service in production.
# IP_GEOLOCATION_API_URL="https://pro.ip-api.com/json/"
# IP_GEOLOCATION_API_KEY="your_pro_api_key"

